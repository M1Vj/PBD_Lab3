# Env Setup
API keys must not be exposed to the public GitHub repository. They are managed through a `.env` file that git ignores. An illustrative `.env.example` has been provided which demonstrates how a user configuring the cloned repo should populate their own token.
