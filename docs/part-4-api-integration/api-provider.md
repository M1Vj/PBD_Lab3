# API Provider
The application is integrated with the `OpenWeatherMap API`. This provides consistent, robust, and reliable weather data, which aligns with industry standards for weather applications. To execute fetches, we require the `VITE_WEATHER_API_KEY` to be set within `.env`.
